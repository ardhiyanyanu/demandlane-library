# OpenTelemetry Collector Configuration
# Sends metrics, logs, and traces to Grafana Cloud via OTLP

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

  # Scrape Spring Boot Actuator metrics
  prometheus:
    config:
      scrape_configs:
        - job_name: 'library-app'
          scrape_interval: 15s
          metrics_path: '/actuator/prometheus'
          static_configs:
            - targets:
              - 'demandlane-library-app-1:8080'
              - 'demandlane-library-app-2:8080'
              - 'demandlane-library-app-3:8080'

processors:
  batch:
    timeout: 10s
    send_batch_size: 1024

  resource:
    attributes:
      - key: service.name
        value: library-management-service
        action: upsert
      - key: deployment.environment
        value: production
        action: upsert

  memory_limiter:
    check_interval: 1s
    limit_mib: 512
    spike_limit_mib: 128

exporters:
  # Send all telemetry to Grafana Cloud via OTLP HTTP
  otlphttp/grafana:
    endpoint: https://otlp-gateway-prod-ap-southeast-2.grafana.net/otlp
    headers:
      Authorization: "Basic MTQ2ODY5MTpnbGNfZXlKdklqb2lNVFl4TnpFeU9DSXNJbTRpT2lKc2FXSnlZWEo1SWl3aWF5STZJalEzVUVVd1JWaG9NakkwUzJJek4zaGpXbTVSYWpaQ09TSXNJbTBpT25zaWNpSTZJbkJ5YjJRdFlYQXRjMjkxZEdobFlYTjBMVElpZlgwPQ=="


  # Debug exporter for troubleshooting (optional)
  debug:
    verbosity: basic

extensions:
  health_check:
    endpoint: 0.0.0.0:13133

service:
  extensions: [health_check]

  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, resource, batch]
      exporters: [otlphttp/grafana, debug]

    metrics:
      receivers: [otlp, prometheus]
      processors: [memory_limiter, resource, batch]
      exporters: [otlphttp/grafana, debug]

    logs:
      receivers: [otlp]
      processors: [memory_limiter, resource, batch]
      exporters: [otlphttp/grafana, debug]

  telemetry:
    logs:
      level: info
    metrics:
      level: detailed

